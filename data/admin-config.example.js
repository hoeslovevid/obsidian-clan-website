/**
 * Example admin password config. Copy to admin-config.js and set your password,
 * OR set ADMIN_EDIT_PASSWORD as a GitHub Actions secret and let the workflow generate it.
 * (admin-config.js is gitignored so your password is never committed.)
 */
var ADMIN_EDIT_PASSWORD = 'change-me';
